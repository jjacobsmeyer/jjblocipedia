<h1><%= @wiki.title %></h1>
<p><%= @wiki.body %></p>
<div class="col-md-8">
  <% if policy(@wiki).edit? %>
    <%= link_to "Edit", edit_wiki_path(@wiki), class: 'btn btn-success' %>
  <% end %>
<div class="col-md-4">
  <% if policy(@wiki).destroy? %>
    <%= link_to "Delete Topic", @wiki, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this topic?' } %>
  <% end %>
</div>
